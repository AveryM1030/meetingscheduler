<h1> The Meeting Has <%= pluralize(@hosts.count, "Host") %></h1>
<table>
	<tbody>
		<% @hosts.each do |host| %>
		<tr>
			<td><%= host.fullname %></td>
			<td><%= host.email %></td>
		</tr>
		<% end %>
	</tbody>
</table>
<a href="/meetings">View Meeting</a>