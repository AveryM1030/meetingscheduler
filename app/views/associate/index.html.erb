<h1>Select Participants to Meeting</h1>
<form action="/assoicate" method="post">
	<div>
		<label>Meeting:</label>
		<select name="assoicate[meeting_id]">
			<option></option>
			<% @meetings.each do |meeting| %>
			<option value="<%= meeting.id %>"><%= meeting.name%></option>
			<% end %>
		</select>
	</div>
	<div>
		<label>Participant:</label>
		<select name="assoicate[participants_id]">
			<option></option>
			<% @participants.each do |participant| %>
			<option value="<%= participant.id %>"><%= participant.fullname%></option>
			<% end %>
		</select>
	</div>
	<input type="submit" value="Add">
</form>
<% if flash[:errors] %>
	<% flash[:errors].each do |e| %>
	<div><%= e %></div>
	<% end %>
<% end %>